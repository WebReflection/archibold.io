#########################
# archibold.io (C) 2016 #
#########################

if [ "$(which sudo 2> /dev/null)" = "" ]; then
  bash <(curl -s archibold.io/install/sudo)
fi

cd tmp
curl -LO archibold.io/utils/server
chmod a+x server
sudo mv server /usr/bin
