#########################
# archibold.io (C) 2016 #
#########################

if [ "$(which sudo 2> /dev/null)" = "" ]; then
  bash <(curl -s archibold.io/install/sudo)
fi

cd tmp
curl -LO archibold.io/utils/open
chmod a+x open
sudo mv open /usr/bin
